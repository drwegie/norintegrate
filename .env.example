# Environment variables are now split per module.
# See each module's .env.example for the variables it needs:
#
#   norintegrate-api/.env.example   — Database, OAuth JWT, CORS, admin emails
#   norintegrate-mcp/.env.example   — Database
#   norintegrate-web/.env.example   — Google OAuth, Auth.js secret, API URL
